# Release Checklist — Emberwilds

_Status as of 2026-07-10. ✅ done · ☐ open · ⚠️ needs a real portal account/build to finish._

## Branding
- ✅ Title "Emberwilds" consistent across browser title, main menu, loading screen
- ✅ Favicon / app icons (`public/icon-192.png`, `public/icon-512.png`) — original
- ✅ Social/OG preview image (`public/og-image.png`)
- ✅ Transparent logo mark (`release/logo/emberwilds-mark-{256,512}.png`) — generated by `scripts/makeIcons.mjs`
- ✅ Metadata + theme color in `index.html`
- ☐ Confirm the name is free of trademark conflict on each target portal before submitting

## Legal
- ✅ Privacy Policy — `release/PRIVACY_POLICY.md`
- ✅ Terms of Service — `release/TERMS_OF_SERVICE.md`
- ✅ Asset & dependency license audit — `release/ASSET_LICENSES.md` (all original + Phaser/MIT; clean)
- ⚠️ Cookie-consent banner — **only needed for the self-hosted emberwilds.fun build** (GA4 + AdSense in the EU). Portals run their own consent, so no banner is needed for portal builds. Add a lightweight CMP before advertising to EU traffic from our own domain.
- ☐ Replace `contact@emberwilds.fun` with a real monitored inbox in both legal docs

## Analytics
- ✅ Centralized manager (`src/systems/analytics.ts`) — gameplay calls `track()`, never a provider directly
- ✅ Player events: game/level start, level clear, level fail (player_death), boss defeated, game complete
- ✅ Engagement events: `settings_changed`, `character_selected`, `level_replayed`
- ✅ Monetization events: `ad_requested`, `interstitial_shown`, `reward_claimed`, `ad_failed` (fired centrally by `AdManager`)

## Monetization
- ✅ Platform SDK abstraction (`src/platform/`) — ads/save routed through one façade
- ✅ `AdManager` (`src/systems/adManager.ts`) — one entry point for ads, analytics-instrumented
- ✅ Interstitial wired between levels (`ClearScene.advance()`) — no-op off-portal, natural break on portals
- ✅ `AdManager.showRewarded()` ready + `rewardedAvailable()` gate. **Design decision pending:** what the reward is (the game uses checkpoints, not lives, so "continue after death" doesn't fit — a "double ember-gems" offer on the clear screen fits the economy). Wire the call once chosen.
- ⚠️ Verify each portal adapter against a real game key (CrazyGames / Poki / Yandex / GameDistribution) — none can run from our own domain

## Technical
- ✅ Mobile: touch controls, responsive scaling, edge-to-edge (no black bars), landscape rotate hint
- ✅ Save system: localStorage with migrations, backup, corruption fallback, **+ platform cloud mirror + fresh-device hydration** (`SaveManager.onSave`, wired in `main.ts` when the platform offers cloud save)
- ✅ Performance: code-split bundle (Phaser vendor / gamedata / game), PWA offline shell
- ✅ Dev-only handles (`__game`, `__platform`) and verbose logging gated behind `import.meta.env.DEV`
- ☐ Final cross-browser pass: Chrome, Safari, Firefox, Edge (desktop + iOS/Android) — needs real hardware
- ☐ Confirm first-playable < 5s on a cold load over 3G-class throttling — needs a measured run

## Publishing
- ✅ Screenshots — `store-assets/screenshots/` (10 × 1600×900), pointed to from `release/screenshots/`
- ✅ Descriptions / taglines / keywords — `release/descriptions/descriptions.md` + `store-assets/STORE_SUBMISSION.md`
- ✅ `/release` submission bundle assembled (logo/, icons/, screenshots/, descriptions/, legal, checklist, security review)
- ⚠️ Per-portal build configs (each portal wants its SDK present and its own build/zip) — produce once accounts exist

## Security
- ✅ Static review done — `release/SECURITY_REVIEW.md` (no secrets, no un-gated debug/logs, no test accounts)
- ☐ Link-check the legal/footer links before each submission

## One-line summary
Everything doable without a live portal account is **done**: branding + logo,
full legal/license/security paperwork, centralized analytics with all event
categories, the ad system wired (interstitial live, rewarded ready), cloud-save
mirroring, and the `/release` bundle. What's left **requires a portal account**
(adapter verification, a rewarded-reward design choice, per-portal builds) or
**real hardware** (cross-browser/device pass) — or is **self-hosted-only** (an
EU cookie banner).
