/**
 * Public entry point. Gameplay imports from here — never from an adapter.
 *   import { Platform } from './platform';
 */
export { Platform } from './Platform';
export type {
  AdResult,
  Capabilities,
  LoadResult,
  PlatformEvent,
  PlatformName,
  Player,
  RewardedResult,
  SaveResult,
} from './types';
